// 함수 선언 또는 함수 정의는 function 키워드로 시작하고, 그 뒤에 함수 이름이 온다. 다음 코드에서 함수 이름은
// logCompliment이다. 함수에 속한 js 코드는 중괄호 사이에 들어간다.
function logCompliment() {
  console.log("잘했어요!");
}

// 선언한 함수를 호출해 실행할 수 있다.
logCompliment();
